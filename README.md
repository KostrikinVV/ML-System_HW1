# 📊 Система мониторинга качества ML-модели в реальном времени

Проект представляет собой упрощенную, но полнофункциональную систему мониторинга для модели машинного обучения, работающую в режиме реального времени. Он построен по принципам микросервисной архитектуры, где каждый сервис выполняет свою узкую задачу и общается с другими через асинхронные сообщения (очередь RabbitMQ).

Система имитирует продвинутый продакшн-пайплайн: генерирует поток данных, делает предсказания, вычисляет ошибки модели и автоматически строит визуализацию. В качестве демонстрации используется задача регрессии для предсказания цен на жилье (California Housing dataset).

---

## 🏗 Архитектура системы

Система состоит из пяти независимых микросервисов (контейнеров Docker), связанных очередями сообщений:

text

                        ┌─────────────┐
                        │   rabbitmq  │  (Брокер сообщений)
                        └──────┬──────┘
                               │
    ┌──────────┐          ┌────▼────┐          ┌──────────┐
    │ features ├──features─►  model ├─y_pred───►  metric  │
    └────┬─────┘ (queue)  └─────────┘ (queue)  └────┬─────┘
         │                                             │
    (y_true queue)                                     ▼
         │                                      metric_log.csv
         └───────────────────────────────────────────┐
                                                     ▼
                                              error_distribution.png
                                                     │
                                               ┌─────┴─────┐
                                               │    plot   │
                                               └───────────┘

**Роли сервисов:**

1. **`rabbitmq`**: Центральный брокер сообщений, обеспечивающий асинхронную и надежную передачу данных между всеми сервисами.
    
2. **`features`**: Генератор данных. В бесконечном цикле выбирает случайные наблюдения из датасета и отправляет векторы признаков и соответствующие им истинные значения в разные очереди, снабжая каждое сообщение уникальным ID.
    
3. **`model`**: Сервис предсказаний. Получает признаки, применяет обученную модель (Random Forest) и отправляет предсказание в очередь, сохраняя исходный ID сообщения.
    
4. **`metric`**: Сервис вычисления метрик. Сопоставляет истинные значения и предсказания по ID, вычисляет абсолютную и относительную ошибки, затем записывает результат в CSV-лог.
    
5. **`plot`**: Сервис визуализации. В реальном времени читает лог с метриками и строит/обновляет график распределения ошибок модели.
    

---

## 🚀 Быстрый старт

Убедитесь, что на вашем компьютере установлены [Docker](https://www.docker.com/get-started) и [Docker Compose](https://docs.docker.com/compose/install/).

1. **Клонируйте репозиторий и перейдите в папку проекта:**
    
    bash
    
    git clone <ваш-репозиторий>
    cd ml-monitoring-system
    
2. **Запустите всю систему одной командой:**
    
    bash
    
    docker-compose up --build
    
    При первом запуске это займет несколько минут (скачаются образы, установятся зависимости).
    
3. **Проверьте работу:**
    
    - Откройте RabbitMQ Management UI: [http://localhost:15672](http://localhost:15672/) (логин: `guest`, пароль: `guest`). Во вкладке **Queues** вы увидите три активные очереди.
        
    - В папке `logs/` начнут появляться файлы `metric_log.csv` и `error_distribution.png`. График будет автоматически обновляться каждые 10 секунд.
        
    - В терминале можно наблюдать логи каждого сервиса в реальном времени.
        
4. **Остановка системы:**  
    Нажмите `Ctrl+C` в терминале, где запущен `docker-compose up`, или выполните в другом окне:
    
    bash
    
    docker-compose down
    

---

## 📁 Описание файлов проекта

### 📦 Корневая директория

- **`docker-compose.yml`** 📄: Главный файл конфигурации. Определяет все сервисы (контейнеры), их настройки, зависимости, сети и тома.
    
- **`Dockerfile`** 📄: Инструкция для сборки базового Docker-образа для Python-сервисов. Устанавливает зависимости из `requirements.txt`.
    
- **`requirements.txt`** 📄: Список всех Python-зависимостей проекта (pika, pandas, scikit-learn и др.).
    
- **`generate_data.py`** 🐍: Вспомогательный скрипт для генерации синтетического датасета `housing.csv` на основе California Housing. **Запускать не нужно**, файл данных уже включен в репозиторий.
    
- **`monitor.py`** 🐍 / **`test_send.py`** 🐍: Вспомогательные скрипты для тестирования и отладки системы (ручная отправка 100 сообщений, простой веб-сервер для просмотра графика).
    

### 🧩 Директории и файлы сервисов

- **`features/features.py`** 🐍: Код сервиса-генератора данных. Загружает датасет, в бесконечном цикле отправляет сообщения в `features_queue` и `y_true_queue`.
    
- **`model/model.py`** 🐍: Код сервиса модели. Обучает RandomForestRegressor на старте, слушает `features_queue`, делает предсказания и отправляет их в `y_pred_queue`.
    
- **`metric/metric.py`** 🐍: Код сервиса метрик. Слушает `y_true_queue` и `y_pred_queue`, сопоставляет сообщения по ID, вычисляет ошибки и записывает их в CSV-файл.
    
- **`plot/plot.py`** 🐍: Код сервиса визуализации. В бесконечном цикле читает CSV-файл, строит 4 графика (гистограммы ошибок, scatter plot и тренд) и сохраняет их в PNG.
    

### 🗃️ Данные и логи

- **`data/housing.csv`** 📊: Датасет, на котором работает система. Содержит признаки и целевую переменную (цена) для предсказания.
    
- **`logs/`** 📁: Директория, которая монтируется в контейнеры. Здесь в реальном времени сохраняются результаты работы:
    
    - `metric_log.csv` — таблица с колонками: `id`, `y_true`, `y_pred`, `absolute_error`, `relative_error`.
        
    - `error_distribution.png` — график, который автоматически обновляется сервисом `plot`.
        

---

## 🛠 Технологический стек

- **Язык и окружение**: Python 3.10, Docker & Docker Compose
    
- **Брокер сообщений**: RabbitMQ 3
    
- **Библиотеки Python**:
    
    - `pika` — для работы с очередями RabbitMQ (AMQP)
        
    - `pandas`, `scikit-learn`, `numpy` — для работы с данными и ML
        
    - `matplotlib` — для построения графиков
        
- **Архитектура**: Микросервисы, асинхронная коммуникация, событийно-ориентированная архитектура.
    

---

## 📊 Ожидаемый результат работы

После успешного запуска вы получите живую, самообновляющуюся систему мониторинга:

1. **Логи в реальном времени**: В терминале будут отображаться сообщения о каждом отправленном наблюдении, сделанном предсказании и вычисленной ошибке.
    
2. **Накопление метрик**: Файл `logs/metric_log.csv` будет постепенно пополняться новыми строками.
    
3. **Автоматическая визуализация**: График `logs/error_distribution.png` будет перестраиваться каждые 10 секунд, отображая текущее распределение ошибок модели, что позволяет сразу оценить её качество и стабильность.
    
4. **Управление очередями**: Интерфейс RabbitMQ (`http://localhost:15672`) позволит в реальном времени наблюдать за потоком сообщений между сервисами.

### 👀 Пример результатов
![[Pasted image 20260115145058.png]]
![[Pasted image 20260115145147.png]]
